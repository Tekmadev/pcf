import ContactHeader from "./ContactHeader";
import Navigation from "./Navigation";

const Header = () => {
  return (
    <header>
      <ContactHeader />
      <Navigation />
    </header>
  );
};

export default Header;
